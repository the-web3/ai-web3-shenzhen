# JSON UI 组件系统完整指南

## 📋 组件预览页面

访问 **http://localhost:3000/preview** 查看所有 JSON 驱动的 UI 组件。

---

## 🎯 支持的组件类型

### 1. Problem（问题分析）

显示为 **Alert 组件**，用于提示用户注意事项、风险或问题。

#### 支持的类型
- `info` - 信息提示（蓝色）
- `warning` - 警告提示（橙色）
- `error` - 错误提示（红色）

#### JSON 示例
```json
{
  "problem": {
    "type": "warning",
    "title": "检测到价格波动",
    "description": "当前市场波动较大，兑换价格可能在交易确认期间发生变化。",
    "suggestions": [
      "建议设置较高的滑点容差",
      "考虑分批兑换以降低风险",
      "查看流动性池深度"
    ]
  }
}
```

#### 渲染效果
- 顶部显示标题（加粗）
- 中间显示描述文字
- 底部显示建议列表（如果有）
- 根据类型显示不同颜色和图标

---

### 2. Operation（操作提案）

显示为 **Card 组件**，展示需要用户确认的操作详情。

#### 支持的字段
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `action` | string | ✅ | 操作类型（transfer/swap/stake等） |
| `asset` | string | - | 资产名称（如 USDT、ETH） |
| `amount` | number | - | 金额 |
| `recipient` | string | - | 接收地址（转账时需要） |
| `chainId` | number | - | 区块链网络 ID |
| `gasEstimate` | string | - | Gas 费用估算 |
| `parameters` | object | - | 自定义参数对象 |

#### JSON 示例
```json
{
  "operation": {
    "action": "transfer",
    "asset": "USDT",
    "amount": 100,
    "recipient": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "chainId": 1,
    "gasEstimate": "0.003 ETH (~$8.50)",
    "parameters": {
      "deadline": 1706083200000,
      "slippage": "0.5%",
      "priorityFee": "2 gwei"
    }
  }
}
```

#### 渲染效果
- **顶部**: 标题 "操作确认" + CheckCircle 图标
- **主体**: Grid 布局显示所有字段
  - 操作类型（大写显示）
  - 资产名称
  - 金额（h6 大小加粗）
  - 接收地址（等宽字体，可换行）
  - 网络（显示为 Chip）
  - 预计费用
  - 参数详情（灰色背景框）
- **底部**: 两个按钮
  - "确认操作" - 靛蓝色实心按钮
  - "取消" - 粉色描边按钮

---

### 3. Supplement（补充信息）

显示为 **Card 组件**，提供辅助决策的参考信息。

#### 支持的字段

##### 3.1 Price Data（价格数据）
```json
{
  "priceData": {
    "symbol": "ETH",
    "currentPrice": 2301.50,
    "change24h": -2.3
  }
}
```
**渲染效果**:
- 大字号显示当前价格
- Chip 显示 24h 涨跌幅
- 涨：绿色 + TrendingUp 图标
- 跌：红色 + TrendingDown 图标

##### 3.2 Risk Score（风险评分）
```json
{
  "riskScore": 45
}
```
**渲染效果**:
- 进度条显示风险等级（0-100）
- 颜色：
  - 0-40: 绿色渐变（低风险）
  - 41-70: 橙色渐变（中风险）
  - 71-100: 红色渐变（高风险）
- 右侧显示数字

##### 3.3 News（新闻列表）
```json
{
  "news": [
    {
      "title": "ETH 市场更新",
      "summary": "以太坊在近期波动后显示出盘整模式。",
      "url": "https://...",
      "timestamp": "2026-01-24T12:00:00Z"
    }
  ]
}
```
**渲染效果**:
- 卡片式新闻列表（最多显示 3 条）
- 可点击跳转（如果有 URL）
- Hover 效果：边框变色 + 向上移动

##### 3.4 Alternatives（替代方案）
```json
{
  "alternatives": [
    "使用 Layer 2 降低费用",
    "批量转账节省成本",
    "等待 Gas 更低时段"
  ]
}
```
**渲染效果**:
- Chip 列表显示
- 粉色描边样式
- 可换行显示

---

## 📦 完整示例

### 示例 1: 转账操作（完整三段式）
```json
{
  "problem": {
    "type": "info",
    "title": "转账请求已识别",
    "description": "您即将执行一笔代币转账操作，请仔细核对以下信息。",
    "suggestions": [
      "请确认接收地址准确无误",
      "确保账户余额充足",
      "当前网络 Gas 费用较低，是转账的好时机"
    ]
  },
  "operation": {
    "action": "transfer",
    "asset": "USDT",
    "amount": 100,
    "recipient": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "chainId": 1,
    "gasEstimate": "0.003 ETH (~$8.50)",
    "parameters": {
      "deadline": 1706083200000,
      "slippage": "0.5%",
      "priorityFee": "2 gwei"
    }
  },
  "supplement": {
    "priceData": {
      "symbol": "USDT",
      "currentPrice": 1.0,
      "change24h": 0.02
    },
    "riskScore": 25,
    "news": [
      {
        "title": "Tether 稳定性报告",
        "summary": "USDT 继续保持与美元的 1:1 锚定。",
        "timestamp": "2026-01-24T12:00:00Z"
      }
    ],
    "alternatives": [
      "使用 Layer 2 降低费用",
      "批量转账节省成本"
    ]
  }
}
```

### 示例 2: 仅错误提示
```json
{
  "problem": {
    "type": "error",
    "title": "余额不足",
    "description": "您的账户余额不足以完成此次交易。",
    "suggestions": [
      "从交易所转入资金",
      "减少交易金额"
    ]
  }
}
```

### 示例 3: 仅价格查询
```json
{
  "supplement": {
    "priceData": {
      "symbol": "BTC",
      "currentPrice": 45230.80,
      "change24h": 3.7
    },
    "news": [
      {
        "title": "Bitcoin 市场分析",
        "summary": "比特币在关键支撑位上方维持强劲走势。",
        "timestamp": "2026-01-24T12:00:00Z"
      }
    ]
  }
}
```

---

## 🎨 UI 组件展示

| 组件 | 用途 | 颜色 |
|------|------|------|
| Alert | 问题/风险提示 | 蓝/橙/红 |
| Card | 操作确认 | 靛蓝边框 |
| Card | 补充信息 | 粉色边框 |
| Chip | 标签/徽章 | 多色 |
| Button | 操作按钮 | 靛蓝/粉色 |
| Typography | 文本显示 | 多级 |
| Grid | 布局 | - |
| Divider | 分隔线 | 浅灰 |
| Progress Bar | 风险评分 | 渐变 |

---

## 🚀 使用场景

### 1. 转账（Transfer）
- Problem: 提示检查地址
- Operation: 显示转账详情
- Supplement: 价格、风险、新闻

### 2. 兑换（Swap）
- Problem: 警告价格波动
- Operation: 显示兑换详情
- Supplement: 价格、滑点、替代方案

### 3. 质押（Stake）
- Problem: 说明锁定期
- Operation: 显示质押详情
- Supplement: APY、替代方案

### 4. 错误处理
- Problem: 显示错误原因
- 无 Operation
- 无 Supplement

### 5. 信息查询
- 无 Problem
- 无 Operation
- Supplement: 仅显示查询结果

---

## 📊 组件预览页功能

1. **选项卡切换** - 查看 6 个不同场景
2. **实时预览** - 查看渲染效果
3. **JSON 查看** - 点击按钮输出到控制台
4. **交互测试** - 点击确认/取消按钮
5. **Schema 参考** - 完整 JSON 结构说明

---

## 🔧 开发指南

### 前端集成
```typescript
import JSONUIRenderer from '@/components/JSONUIRenderer';
import { AIResponse } from '@/types';

const MyComponent = () => {
  const aiResponse: AIResponse = {
    // ... 你的 JSON 数据
  };

  const handleConfirm = (operation: Operation) => {
    console.log('确认操作:', operation);
    // 执行区块链交易
  };

  const handleCancel = () => {
    console.log('取消操作');
  };

  return (
    <JSONUIRenderer
      data={aiResponse}
      onConfirm={handleConfirm}
      onCancel={handleCancel}
    />
  );
};
```

### 后端响应示例
```go
// Go 后端
type AIResponse struct {
    Message    string       `json:"message"`
    AIResponse *AIStructure `json:"aiResponse,omitempty"`
}

response := AIResponse{
    Message: "AI 已处理您的请求",
    AIResponse: &AIStructure{
        Problem: &ProblemAnalysis{
            Type: "info",
            Title: "转账请求",
            // ...
        },
        // ...
    },
}
```

---

## ✨ 特色功能

1. **三段式可选** - problem、operation、supplement 均可单独使用
2. **自适应布局** - 根据内容自动调整
3. **现代风格** - 圆润、渐变、动画
4. **交互友好** - Hover 效果、点击反馈
5. **类型安全** - TypeScript 完整支持

---

访问 http://localhost:3000/preview 立即体验！
